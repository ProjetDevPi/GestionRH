{% extends 'base.html.twig' %}

{% block container %}
    <h1>Matiere</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Nommatiere</th>
                <td>{{ matiere.nommatiere }}</td>
            </tr>
            <tr>
                <th>Coef</th>
                <td>{{ matiere.coef }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('matiere_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('matiere_edit', { 'id': matiere.id }) }}">Edit</a>
        </li>
        <li>

            <div>
                <a href="{{ path('delete1', { 'id': matiere.id }) }}" class="btn btn-danger btn-round">delete</a>
            </div>

        </li>
    </ul>
{% endblock %}
